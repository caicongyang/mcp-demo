# Server settings
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8000

# Database settings
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=your_database

# Logging settings
LOG_LEVEL=INFO

# Security settings (optional)
# Set to "true" to only allow SELECT queries
RESTRICT_TO_SELECT_ONLY=false

# Connection pool settings (optional)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30